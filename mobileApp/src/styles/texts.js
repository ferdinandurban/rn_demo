import React, {StyleSheet} from 'react-native';

const texts = StyleSheet.create({
  subheader: {
    flex: 0,
    marginTop: 10,
    marginLeft: 10,
    marginBottom: 5
  }
});

export default texts;
